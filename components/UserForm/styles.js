import styled from "styled-components";

export default styled.div`
  border: 1px solid var(--gray-1);
  padding: 40px;
  max-width: 500px;
  margin: 0 auto;
  border-radius: 3px;

  button[type="submit"] {
    margin-top: 25px;
  }
`;
